// backend/utils/sendMessage.js

const sendMessage = async (phoneNumber, message) => {
    console.log(`Sending SMS to ${phoneNumber}: ${message}`);
    // Implement your SMS service integration here
  };
  
  module.exports = sendMessage;
  